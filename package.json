{
  "name": "roo-mini",
  "version": "1.0.0",
  "description": "Lightweight AI coding assistant with MCP support and multi-agent orchestration",
  "type": "module",
  "main": "src/index.mjs",
  "scripts": {
    "start": "node src/index.mjs",
    "dev": "node src/index.mjs -v",
    "setup": "node src/config/setup.mjs",
    "costs": "node scripts/costs.mjs",
    "test": "node --test tests/*.test.mjs",
    "lint": "eslint src/ tests/",
    "check": "npm run lint && npm test",
    "version:current": "node scripts/version.mjs",
    "version:patch": "node scripts/version.mjs patch",
    "version:minor": "node scripts/version.mjs minor",
    "version:major": "node scripts/version.mjs major",
    "version:tag": "node -p \"require('./package.json').version\" | xargs -I {} git tag v{} && git push --tags",
    "version:bump-release": "npm run version:patch && git add -A && git commit -m \"chore: bump version to $(node -p \"require('./package.json').version\")\" && git tag v$(node -p \"require('./package.json').version\") && git push && git push --tags"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "boxen": "^7.1.1",
    "chalk": "^5.3.0",
    "diff": "^9.0.0",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "glob": "^10.4.5",
    "googleapis": "^173.0.0",
    "openai": "^4.77.3",
    "pg": "^8.22.0",
    "playwright": "^1.61.1"
  },
  "devDependencies": {
    "eslint": "^10.7.0"
  },
  "keywords": [
    "ai",
    "coding-assistant",
    "mcp",
    "agent",
    "orchestration"
  ],
  "license": "MIT"
}
